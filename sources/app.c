#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "../headers/functions.h"

int main() {
    Node *root = NULL;
    int h;
    
    h = insertAVL(10, &root, h);
    h = insertAVL(10, &root, h);


    return 0;
}